package com.megathirio.shinsei.blocks;

public class ShinseiStones {
/*
    public static HashMap<String, Block> stonesEntries = HashMaps.getStonesMap();

    public static void registerStones(){
        for(Map.Entry<String, Block> stoneEntry : stonesEntries.entrySet()){
            GameRegistry.registerBlock(stoneEntry.getValue(), stoneEntry.getKey());
        }
    }

    public static Block getBlock(String blockName){
        return stonesEntries.get(blockName);
    }
*/
}
